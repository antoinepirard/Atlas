Read CLAUDE.md for project context. Release the changes made by:

1. Check git log for recent commits and determine the next version tag based on previous releases
2. Update version in package.json, src-tauri/tauri.conf.json, and src-tauri/Cargo.toml
3. Commit any uncommitted changes with appropriate message
4. Create and push the version tag
5. Push to main branch
6. Add release notes to the GitHub release based on the changes
